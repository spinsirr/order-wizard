diff --git a/dist/init-yCWd5sZ8.mjs b/dist/init-yCWd5sZ8.mjs
index 3ebd6d5d6c30392b04a81dc85d8ec6f08f4533a6..8ffdf09874e82fd9eb70315e6ea84ab979d71caf 100644
--- a/dist/init-yCWd5sZ8.mjs
+++ b/dist/init-yCWd5sZ8.mjs
@@ -44,9 +44,9 @@ var CwsApi = class {
 				"x-goog-api-version": "2"
 			}
 		});
-		if (res.uploadState === "FAILURE") {
+		if (res.uploadState !== "SUCCESS") {
 			const errors = res.itemError?.map((e) => `${e.error_code}: ${e.error_detail}`).join("\n");
-			throw new Error(`Chrome Web Store upload failed:\n${errors}`);
+			throw new Error(`Chrome Web Store upload did not succeed (${res.uploadState ?? "MISSING_STATE"}):\n${errors ?? "No error details returned"}`);
 		}
 	}
 	async submitForReview(params) {
@@ -63,9 +63,8 @@ var CwsApi = class {
 				"Content-Length": "0"
 			}
 		});
-		if (res.uploadState === "FAILURE") {
-			const errors = res.itemError?.map((e) => `${e.error_code}: ${e.error_detail}`).join("\n");
-			throw new Error(`Chrome Web Store publish failed:\n${errors}`);
+		if (!Array.isArray(res.status) || res.status.length === 0 || res.status.some((status) => status !== "OK" && status !== "ITEM_PENDING_REVIEW")) {
+			throw new Error(`Chrome Web Store submission failed (${JSON.stringify(res.status) ?? "MISSING_STATUS"}):\n${res.statusDetail?.join("\n") ?? "No error details returned"}`);
 		}
 	}
 	getToken() {
